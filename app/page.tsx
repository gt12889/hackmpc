import { HeroReveal } from "@/components/hero-reveal";

export const dynamic = "force-dynamic";

// Home is the cinematic brand overview. The live dashboard lives at /dashboard.
export default function HomePage() {
  return <HeroReveal />;
}
